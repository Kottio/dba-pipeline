# mcp/ — Phase 7

Semantic layer (governed metric definitions) + MCP server exposing them to LLMs.
Tools: list_metrics · get_metric · run_checked_query. Read-only DB user, row limits, no PII.
The demo: raw-schema LLM answers vs governed-metric answers, side by side.
