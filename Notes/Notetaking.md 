Personal Notes: You need to look at this and add it formated into docs/ai-log 


## 17/07
Plannification avec Claude de la pipleine. 

-------
Creation de Repo par Claude
+ Creation de dossier important pour github
- Creation de beaucoup de fichiers que je ne comprends pas
  /sqlfluff
  /docker-compose deja ecris??? 
  /.github/workflow creer sans explication ou communications 
- Trop de decision prises sans evolution au fur et a mesure. 

I will ask claude what are those files that i don't underdanst and delete them so we grow the project 
organically. 

Delete Docker Compose, githubWorflow. 
in uv project deleted dependencies written before even working on i. 
sqlfluff will be creqateed when needed. 

Reset the git with git init delted the git created by claude.
-----

Changed to plan to only upload to Droplet when the project wokred locally already to op[timiose cosst]
Clarified that the project shoudl be first updated to github then to droplet. 

-- 
Added the all project plan within the repo as .gitignore to help claude. 
----

Phase 1- Infra/dependencies
Changed the planmnign phase, nothing needed right now docker containers. 
only python dependencies and other tech. 
Had to explain dbt fusion is not python, AI not alway up to date. 


Had to push that dbt fusion works with duckdb. 
Experience was clear advantage to not let claude decide on assumption. 

--------
Testing dbt fusion with ducklake
Creating a test folder
duckdb dev.duckdb 
INSTALL ducklake;

ATTACH 'ducklake:my_ducklake.ducklake' AS my_ducklake;
USE my_ducklake;

CREATE SCHEMA my_lake;
my_ducklake D
my_ducklake D CREATE TABLE my_lake.test_table AS
                SELECT 1 AS id, 'hello ducklake' AS name, now() AS created_at
                UNION ALL
                SELECT 2, 'second row', now();
my_ducklake D
my_ducklake D SELECT * FROM my_lake.test_table;



OK in Profiles we need to attach the ducklake 
  outputs:
    ducklake:
      type: duckdb
      path: dev.duckdb
      attach:
        - path: "ducklake:my_ducklake.ducklake"
          alias: my_ducklake

in project we need to set it to this database fromt he alias. 
models:
  dbt_ducklake:
    +database: my_ducklake


in Source we need to expliclity also set the database 
sources:
  - name: my_lake
    database: my_ducklake
    tables:
      - name: test_table


Accessing the ducklake alway need to be done via duckdb and ATTACH ducklake:my_ducklake.ducklake 

DuckDB     = the engine (a program that runs SQL)
DuckLake   = the memory (Parquet files + a catalog that indexes them)
ATTACH 'ducklake:...'  = the key that connects engine to memory
views live in the catalog · tables live as Parquet


Added a Notetaking cleaning task for claude, reading this notes and cleaning it as well as adinf into docs.

------

 20/07 day 2 
The initial plan of Claude is infromative but too complex and spceific and missing importante part. 
Starting phase 1 locally, Setting duckdb ducklake, keeping track of the code I write following Documentation. 

For infrastrucutre right now I only need duckdb code. 
Putting duckdb code inside script that will go into infra. -> init_lake.sh

Needed dudkdb dependencies things need ot be into a file called setup_server.sh with dbt and duckdb and all technologies who won't be inside uv 
Also need to be inside Readme? Claude unitially forgot to create setupservce,, much easier to keep track when adding to it along the way

create the duckalke setup with specific path for lake files. 

Now investigation uv
curl -LsSf https://astral.sh/uv/install.sh | sh


created mkdir uv/example 
uv init example 
uv add urff uv tst 


Now Starting realy with 
`uv init --bare`

Had to search online to find dlt actually had a dlt[ducklake] and could speak natively to it. 
Claude only new abotu dlt[duckdb]

 `uv add "dlt[postgres, ducklake]"`
source .venv/bin/activate

cd prior-testing/dbt-ducklake
dlt initi foo ducklake

Tried to work on it locally had to set a few files.


PRoblem the catalog path did not match the actuall parquet files of the test datalake


duckdb -c "ATTACH 'ducklake:my_ducklake.ducklake' AS lake (DATA_PATH '/Users/kottio/KottioDev/DBuilders_pip/prior-testing/dbt-ducklake/my_ducklake.ducklake.files/');"
duckdb -c "
ATTACH 'ducklake:my_ducklake.ducklake' AS lake;
CREATE SCHEMA lake.my_lake;
CREATE TABLE lake.my_lake.test_table AS
  SELECT 1 AS id, 'hello ducklake' AS name, now() AS created_at
  UNION ALL SELECT 2, 'second row', now();"

dbt run

secrets toml 

[destination.ducklake.credentials]
ducklake_name = "my_ducklake"
catalog = "duckdb:///my_ducklake.ducklake"

[destination.ducklake.credentials.storage]
bucket_url = "file:///Users/kottio/KottioDev/DBuilders_pip/prior-testing/dbt-ducklake/my_ducklake.ducklake.files"

---
foo pipline 
import dlt

pipeline = dlt.pipeline(
    pipeline_name="foo",
    destination="ducklake",
    dataset_name="lake",
    dev_mode=True,
)

info = pipeline.run(
    [{"foo": 1}, {"foo": 2}],
    table_name="table_foo",
)
print(info)
print(pipeline.dataset().table_foo["foo"].df())

---


Can be setting the dev mode to false to not have schema.. 


Finished the Sandbox, 
To test now will create dbt pip to touch foo. 

Ok All worked make sure to create in the same schema. 

Could do this version: 2

sources:
  - name: my_lake                 # source name (what you write in source())
    database: my_ducklake
    schema: my_lake               # physical schema in the lake
    tables:
      - name: test_table

  - name: dlt_raw                 # second source, different schema
    database: my_ducklake
    schema: lake_schema_20260720055955   # (the timestamped one, for now)
    tables:
      - name: table_foo

      and ref different source. 



----------------
21/07
Redid the the pipeline from scratch struggled a bit. 

NEED the creation of ducklake manuallym, settting as well the file path manually so more clean.
Then in the dlt, need to reference this path well. 
Setting uv
`uv init --bare`
` uv add "dlt[ducklake]"`
`uv run dlt init foo ducklake`


Two option work Relative path and Aboslut path 

Absolut
### With Absolut Path $(pwd)/data/lake_files/
```bash
mdkir data

duckdb ATTACH 'ducklake:data/my_ducklake.ducklake' as lake; #Without Data Path

IN DLT config 

[destination.ducklake.credentials]
ducklake_name = "my_ducklake"
catalog = "duckdb:///data/my_ducklake.ducklake" 

[destination.ducklake.credentials.storage]
bucket_url = "file:///Users/kottio/KottioDev/DBuilders_pip/prior-testing/SecondFullTest/data/my_ducklake.ducklake.files/"
```


### With Relative Path
```bash
ATTACH 'ducklake:data/lake.ducklake' AS lake (DATA_PATH 'data/lake_files')


[destination.ducklake.credentials]
ducklake_name = "lake"
catalog = "duckdb:///data/lake.ducklake"

  

[destination.ducklake.credentials.storage]
bucket_url = "file:data/lake_files/"


#And then this showed me the dlt
attach 'ducklake:data/lake.ducklake' as lake;
memory D use lake;
lake D .tables;

#The lake_files folder is in the repo data/ 

```

Claude was struggling a lot witrht his needed to test myself for it t o work. 



--------------
22/07

Made the dbt work. 
Several things needed to be considered. 
Claude was lost, better to use github of well configured project.

Important learning -> The db duckdb used by dbt to access the ducklake need to have a different name 
than the lake itself...


Finally
- Created the ducklake 
- created dlt pipipline wirting into it
- Created dbt project reading and writing into it.
Full note in obsidian knowledge center for mentee. 



need to implement absolute path with {{pwd}} and jsutify? 





Implement just file executing infre set script with abolut path! 

just init-lake in main repo
----

Starting the neondb connexion 
psql 'postgresql://analytics_ro:PWDcfv@ep-tiny-resonance-abow3qnr-pooler.eu-west-2.aws.neon.tech/databuilder_db?sslmode=require&channel_binding=require' -c "SELECT count(*) FROM event;"

CREATE ROLE analytics_ro WITH LOGIN PASSWORD 'secret'
grant usage on schema public to analytics_ro;
grant select on all tables in schema public to analytics_ro;
alter default privileges in schema public grant select on tables to analytics_ro;

uv run dlt init sql_database ducklake


ALL the config.toml of dlt are now part of .env

uv add "dlt[sql_database]"

just ingest                # from anywhere in the repo — just finds the justfile,
Work becasue load_dotenv() in justfile 


set -a; source .env; set +a       # set -a = "export everything I source"
uv run python ingestion/sql_database_pipeline.py




OK so replace toml secret by aboslut path in env var. 
To use need to export env variable from env. 
In the dlt pipline can remove credentials of sources as automatically coming from toml which check env first. 


dlt extracted students.

