# infra/ — Phase 1

Everything needed to reproduce the platform: docker compose, server setup notes, grants scripts (secrets excluded).
Target: one small DO droplet, everything in containers, cost tracked in the root README.
